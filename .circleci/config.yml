version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run:
          name: compile_and_run
          command: sudo apt-get update & sudo apt-get install make & make test
